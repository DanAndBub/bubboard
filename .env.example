# Admin API authentication
# Generate a 32+ character random string (e.g.: openssl rand -hex 32)
# Both values MUST be the same string
DRIFTWATCH_ADMIN_SECRET=
NEXT_PUBLIC_ADMIN_SECRET=

# Provider admin API keys (for org-wide usage/billing data)
ANTHROPIC_ADMIN_KEY= [REDACTED]
OPENAI_ADMIN_KEY=
